---
const BETA_CODE = import.meta.env.PUBLIC_BETA_CODE;
const WEBHOOK_URL = import.meta.env.PUBLIC_N8N_WEBHOOK_URL;
const UPLOAD_URL = import.meta.env.PUBLIC_UPLOAD_URL;
---

<div id="ff-overlay" role="dialog" aria-modal="true" aria-label="Formulaire de candidature Flaynn" hidden>

  <!-- Honeypot -->
  <input type="text" name="website" id="ff-honeypot" tabindex="-1" autocomplete="off" aria-hidden="true" style="display:none!important;position:absolute;left:-9999px">

  <!-- Header chrome -->
  <header class="ff-header">
    <div class="ff-progress-track" aria-hidden="true">
      <div id="ff-bar" class="ff-bar"></div>
    </div>
    <div class="ff-header-meta">
      <span class="ff-brand">Flaynn</span>
      <span id="ff-step-label" class="ff-step-label">Accès</span>
    </div>
  </header>

  <!-- Steps -->
  <main id="ff-steps" class="ff-main">

    <!-- 0 · Code bêta -->
    <div class="ff-step" data-step="0">
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Accès restreint</p>
        <h2 class="ff-q">Code d'accès bêta</h2>
        <p class="ff-hint">Flaynn est en accès restreint. Entrez votre code pour continuer.</p>
        <input type="text" id="ff-0" class="ff-input" placeholder="••••••••••" autocomplete="off" aria-label="Code d'accès bêta" />
        <div class="ff-error" id="ff-err-0" role="alert"></div>
        <nav class="ff-nav">
          <div></div>
          <button class="ff-btn-next ff-btn-primary" data-step="0">Accéder</button>
        </nav>
      </div>
    </div>

    <!-- 1 · Nom fondateur -->
    <div class="ff-step" data-step="1" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Informations de contact</p>
        <label class="ff-q" for="ff-1">Nom complet du fondateur</label>
        <input type="text" id="ff-1" class="ff-input" autocomplete="name" aria-required="true" placeholder="Prénom Nom" />
        <div class="ff-error" id="ff-err-1" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="1">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="1">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 2 · Email -->
    <div class="ff-step" data-step="2" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Informations de contact</p>
        <label class="ff-q" for="ff-2">Email professionnel</label>
        <input type="email" id="ff-2" class="ff-input" autocomplete="email" aria-required="true" placeholder="vous@startup.com" />
        <div class="ff-error" id="ff-err-2" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="2">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="2">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 3 · Pays -->
    <div class="ff-step" data-step="3" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Informations de contact</p>
        <label class="ff-q" for="ff-3">Pays</label>
        <input type="text" id="ff-3" class="ff-input" autocomplete="country-name" aria-required="true" placeholder="France" />
        <div class="ff-error" id="ff-err-3" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="3">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="3">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 4 · Ville -->
    <div class="ff-step" data-step="4" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Informations de contact</p>
        <label class="ff-q" for="ff-4">Ville</label>
        <input type="text" id="ff-4" class="ff-input" autocomplete="address-level2" aria-required="true" placeholder="Paris" />
        <div class="ff-error" id="ff-err-4" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="4">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="4">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 5 · Téléphone (optionnel) -->
    <div class="ff-step" data-step="5" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Informations de contact</p>
        <label class="ff-q" for="ff-5">Numéro de téléphone <span class="ff-opt">— optionnel</span></label>
        <input type="tel" id="ff-5" class="ff-input" autocomplete="tel" placeholder="+33 6 00 00 00 00" />
        <div class="ff-error" id="ff-err-5" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="5">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="5">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 6 · Nom startup -->
    <div class="ff-step" data-step="6" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">L'Accroche</p>
        <label class="ff-q" for="ff-6">Nom de la startup</label>
        <input type="text" id="ff-6" class="ff-input" aria-required="true" placeholder="Acme Inc." />
        <div class="ff-error" id="ff-err-6" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="6">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="6">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 7 · Pitch une phrase -->
    <div class="ff-step" data-step="7" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">L'Accroche</p>
        <label class="ff-q" for="ff-7">En une phrase, que fait votre startup ?</label>
        <div class="ff-field-wrap">
          <input type="text" id="ff-7" class="ff-input" maxlength="140" aria-required="true" placeholder="Nous aidons [qui] à [quoi] grâce à [comment]." />
          <span class="ff-counter" id="ff-count-7">0 / 140</span>
        </div>
        <div class="ff-error" id="ff-err-7" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="7">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="7">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 8 · Problème -->
    <div class="ff-step" data-step="8" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">L'Accroche</p>
        <label class="ff-q" for="ff-8">Le Problème</label>
        <div class="ff-field-wrap">
          <textarea id="ff-8" class="ff-textarea" maxlength="400" rows="5" aria-required="true" placeholder="Décrivez le point de douleur de vos clients…"></textarea>
          <span class="ff-counter" id="ff-count-8">0 / 400</span>
        </div>
        <div class="ff-error" id="ff-err-8" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="8">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="8">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 9 · Solution -->
    <div class="ff-step" data-step="9" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">L'Accroche</p>
        <label class="ff-q" for="ff-9">Comment résolvez-vous ce problème ?</label>
        <div class="ff-field-wrap">
          <textarea id="ff-9" class="ff-textarea" maxlength="400" rows="5" aria-required="true" placeholder="Votre solution en détail…"></textarea>
          <span class="ff-counter" id="ff-count-9">0 / 400</span>
        </div>
        <div class="ff-error" id="ff-err-9" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="9">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="9">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 10 · Secteur -->
    <div class="ff-step" data-step="10" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-10">Secteur principal</label>
        <input type="text" id="ff-10" class="ff-input" placeholder="LegalTech, FinTech B2B, SaaS RH…" aria-required="true" />
        <div class="ff-error" id="ff-err-10" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="10">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="10">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 11 · Type client -->
    <div class="ff-step" data-step="11" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-11">Type de client cible</label>
        <input type="text" id="ff-11" class="ff-input" placeholder="PME françaises, DSI grands groupes…" aria-required="true" />
        <div class="ff-error" id="ff-err-11" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="11">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="11">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 12 · TAM -->
    <div class="ff-step" data-step="12" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-12">Marché Total Adressable (TAM) en USD</label>
        <input type="number" id="ff-12" class="ff-input" placeholder="2 000 000 000" min="0" aria-required="true" />
        <div class="ff-error" id="ff-err-12" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="12">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="12">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 13 · Sources TAM -->
    <div class="ff-step" data-step="13" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-13">Comment avez-vous estimé ce chiffre ?</label>
        <textarea id="ff-13" class="ff-textarea" rows="5" aria-required="true" placeholder="Sources, méthodologie, études de marché…"></textarea>
        <div class="ff-error" id="ff-err-13" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="13">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="13">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 14 · Acquisition clients (optionnel) -->
    <div class="ff-step" data-step="14" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-14">Comment acquérez-vous vos clients ? <span class="ff-opt">— optionnel</span></label>
        <textarea id="ff-14" class="ff-textarea" rows="5" placeholder="Canaux, stratégie, coût d'acquisition…"></textarea>
        <div class="ff-error" id="ff-err-14" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="14">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="14">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 15 · Concurrents (optionnel) -->
    <div class="ff-step" data-step="15" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Le Moteur</p>
        <label class="ff-q" for="ff-15">Qui cherchez-vous à détrôner ? <span class="ff-opt">— optionnel</span></label>
        <textarea id="ff-15" class="ff-textarea" rows="5" placeholder="Concurrents directs et votre avantage différentiel…"></textarea>
        <div class="ff-error" id="ff-err-15" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="15">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="15">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 16 · Stade -->
    <div class="ff-step" data-step="16" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <p class="ff-q">À quel stade êtes-vous aujourd'hui ?</p>
        <div class="ff-choices" id="ff-16" role="group" aria-label="Stade">
          <button type="button" class="ff-choice" data-value="Idée">Idée</button>
          <button type="button" class="ff-choice" data-value="MVP">MVP</button>
          <button type="button" class="ff-choice" data-value="Pré-revenue">Pré-revenue</button>
          <button type="button" class="ff-choice" data-value="Pré-seed">Pré-seed</button>
          <button type="button" class="ff-choice" data-value="Seed">Seed</button>
          <button type="button" class="ff-choice" data-value="Série A+">Série A+</button>
        </div>
        <div class="ff-error" id="ff-err-16" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="16">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="16">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 17 · Revenus -->
    <div class="ff-step" data-step="17" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <p class="ff-q">Générez-vous des revenus ?</p>
        <div class="ff-choices ff-choices--duo" id="ff-17" role="group" aria-label="Revenus">
          <button type="button" class="ff-choice" data-value="Oui">Oui</button>
          <button type="button" class="ff-choice" data-value="Non">Non</button>
        </div>
        <div class="ff-error" id="ff-err-17" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="17">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="17">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 18 · MRR -->
    <div class="ff-step" data-step="18" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <label class="ff-q" for="ff-18">Revenu mensuel — MRR en $ <span id="ff-18-req-label"></span></label>
        <input type="number" id="ff-18" class="ff-input" min="0" placeholder="0" />
        <div class="ff-error" id="ff-err-18" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="18">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="18">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 19 · Clients payants -->
    <div class="ff-step" data-step="19" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <label class="ff-q" for="ff-19">Nombre de clients payants <span id="ff-19-req-label"></span></label>
        <input type="number" id="ff-19" class="ff-input" min="0" placeholder="0" />
        <div class="ff-error" id="ff-err-19" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="19">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="19">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 20 · Pourquoi vous -->
    <div class="ff-step" data-step="20" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <label class="ff-q" for="ff-20">Pourquoi VOUS ?</label>
        <div class="ff-field-wrap">
          <textarea id="ff-20" class="ff-textarea" maxlength="500" rows="5" aria-required="true" placeholder="Présentez l'équipe fondatrice et votre avantage compétitif…"></textarea>
          <span class="ff-counter" id="ff-count-20">0 / 500</span>
        </div>
        <div class="ff-error" id="ff-err-20" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="20">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="20">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 21 · Temps plein -->
    <div class="ff-step" data-step="21" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <p class="ff-q">L'équipe est-elle à temps plein ?</p>
        <div class="ff-choices ff-choices--duo" id="ff-21" role="group" aria-label="Temps plein">
          <button type="button" class="ff-choice" data-value="Oui">Oui</button>
          <button type="button" class="ff-choice" data-value="Non">Non</button>
        </div>
        <div class="ff-error" id="ff-err-21" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="21">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="21">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 22 · Priorité 6 mois -->
    <div class="ff-step" data-step="22" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Exécution &amp; Équipe</p>
        <label class="ff-q" for="ff-22">Priorité des 6 prochains mois</label>
        <textarea id="ff-22" class="ff-textarea" rows="5" aria-required="true" placeholder="Votre objectif numéro un…"></textarea>
        <div class="ff-error" id="ff-err-22" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="22">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="22">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 23 · Montant levée -->
    <div class="ff-step" data-step="23" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">La Demande</p>
        <p class="ff-q">Combien souhaitez-vous lever ?</p>
        <div class="ff-choices" id="ff-23" role="group" aria-label="Montant levée">
          <button type="button" class="ff-choice" data-value="< 100k€">&lt;&nbsp;100k€</button>
          <button type="button" class="ff-choice" data-value="100k–500k€">100k – 500k€</button>
          <button type="button" class="ff-choice" data-value="500k–1M€">500k – 1M€</button>
          <button type="button" class="ff-choice" data-value="1M–3M€">1M – 3M€</button>
          <button type="button" class="ff-choice" data-value="> 3M€">&gt;&nbsp;3M€</button>
        </div>
        <div class="ff-error" id="ff-err-23" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="23">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="23">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 24 · Jalons 18 mois -->
    <div class="ff-step" data-step="24" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">La Demande</p>
        <label class="ff-q" for="ff-24">Jalons clés — 18 prochains mois</label>
        <textarea id="ff-24" class="ff-textarea" rows="5" aria-required="true" placeholder="Milestones produit, revenus, équipe…"></textarea>
        <div class="ff-error" id="ff-err-24" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="24">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="24">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 25 · Utilisation fonds (optionnel) -->
    <div class="ff-step" data-step="25" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">La Demande</p>
        <label class="ff-q" for="ff-25">Utilisation prévue des fonds <span class="ff-opt">— optionnel</span></label>
        <textarea id="ff-25" class="ff-textarea" rows="5" placeholder="R&D, recrutement, marketing…"></textarea>
        <div class="ff-error" id="ff-err-25" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="25">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="25">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 26 · Vision 5 ans -->
    <div class="ff-step" data-step="26" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">La Demande</p>
        <label class="ff-q" for="ff-26">À quoi ressemble votre entreprise dans 5 ans ?</label>
        <textarea id="ff-26" class="ff-textarea" rows="5" aria-required="true" placeholder="Votre vision long terme…"></textarea>
        <div class="ff-error" id="ff-err-26" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="26">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="26">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 27 · Pitch Deck -->
    <div class="ff-step" data-step="27" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Documents</p>
        <label class="ff-q" for="ff-27">Votre Pitch Deck</label>
        <p class="ff-hint">PDF uniquement · Max 15 Mo</p>
        <div class="ff-dropzone" id="ff-file-zone-27">
          <input type="file" id="ff-27" class="ff-file-input" accept=".pdf" aria-required="true" aria-label="Pitch Deck PDF" />
          <div class="ff-drop-idle" id="ff-drop-idle-27">
            <div class="ff-drop-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="12" y1="18" x2="12" y2="12"/><line x1="9" y1="15" x2="15" y2="15"/></svg>
            </div>
            <p class="ff-drop-label">Déposer le fichier ici</p>
            <p class="ff-drop-sub">ou <span class="ff-drop-link">parcourir</span></p>
          </div>
          <div class="ff-drop-active" id="ff-file-info-27" hidden>
            <div class="ff-file-row">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
              <span class="ff-file-name" id="ff-file-name-27"></span>
            </div>
            <div class="ff-upload-track"><div class="ff-upload-fill" id="ff-upload-bar-27"></div></div>
            <p class="ff-file-status" id="ff-file-status-27"></p>
          </div>
        </div>
        <div class="ff-error" id="ff-err-27" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="27">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="27">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 28 · Lien démo (optionnel) -->
    <div class="ff-step" data-step="28" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Documents</p>
        <label class="ff-q" for="ff-28">Lien vidéo de démo <span class="ff-opt">— optionnel</span></label>
        <input type="url" id="ff-28" class="ff-input" placeholder="https://www.loom.com/share/…" />
        <div class="ff-error" id="ff-err-28" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="28">← Retour</button>
          <button class="ff-btn-next ff-btn-primary" data-step="28">Continuer</button>
        </nav>
      </div>
    </div>

    <!-- 29 · Doc supplémentaire (optionnel) -->
    <div class="ff-step" data-step="29" hidden>
      <div class="ff-step-inner">
        <p class="ff-eyebrow">Documents</p>
        <label class="ff-q" for="ff-29">Document complémentaire <span class="ff-opt">— optionnel</span></label>
        <p class="ff-hint">PDF, Office, JPG, PNG · Max 15 Mo</p>
        <div class="ff-dropzone" id="ff-file-zone-29">
          <input type="file" id="ff-29" class="ff-file-input" accept=".pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.jpg,.png" aria-label="Document supplémentaire" />
          <div class="ff-drop-idle" id="ff-drop-idle-29">
            <div class="ff-drop-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="12" y1="18" x2="12" y2="12"/><line x1="9" y1="15" x2="15" y2="15"/></svg>
            </div>
            <p class="ff-drop-label">Déposer le fichier ici</p>
            <p class="ff-drop-sub">ou <span class="ff-drop-link">parcourir</span></p>
          </div>
          <div class="ff-drop-active" id="ff-file-info-29" hidden>
            <div class="ff-file-row">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
              <span class="ff-file-name" id="ff-file-name-29"></span>
            </div>
            <div class="ff-upload-track"><div class="ff-upload-fill" id="ff-upload-bar-29"></div></div>
            <p class="ff-file-status" id="ff-file-status-29"></p>
          </div>
        </div>
        <div class="ff-error" id="ff-err-29" role="alert"></div>
        <nav class="ff-nav">
          <button class="ff-btn-back" data-step="29">← Retour</button>
          <button class="ff-btn-next ff-btn-submit ff-btn-primary" data-step="29">Soumettre mon dossier</button>
        </nav>
      </div>
    </div>

    <!-- 30 · Écran final -->
    <div class="ff-step ff-step--final" data-step="30" hidden>
      <div class="ff-step-inner">
        <div id="ff-final-spinner" class="ff-final-state">
          <div class="ff-spinner"></div>
          <p class="ff-final-label">Envoi en cours…</p>
        </div>
        <div id="ff-final-success" class="ff-final-state" hidden>
          <div class="ff-success-glyph">✦</div>
          <h2 class="ff-success-title">Dossier reçu.</h2>
          <p class="ff-success-body">Notre comité analyse votre dossier sous 24h.<br>Vous recevrez votre rapport sur <strong id="ff-success-email"></strong>.</p>
        </div>
        <div id="ff-final-error" class="ff-final-state" hidden>
          <p class="ff-err-body">Une erreur est survenue. Réessayez ou contactez<br><a href="mailto:contact@flaynn.fr">contact@flaynn.fr</a></p>
          <button class="ff-btn-retry" id="ff-retry">Réessayer</button>
        </div>
      </div>
    </div>

  </main>

  <!-- Keyboard hint -->
  <div class="ff-kbd-bar" id="ff-kbd-bar" aria-hidden="true">
    Appuyer sur <kbd>Entrée ↵</kbd> pour continuer
  </div>

  <!-- Close -->
  <button class="ff-close" id="ff-close" aria-label="Fermer">
    <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M1 1l12 12M13 1L1 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
  </button>

</div>

<style>
/* ─── Reset + variables locales ──────────────────────────── */
#ff-overlay *,#ff-overlay *::before,#ff-overlay *::after{box-sizing:border-box;margin:0;padding:0}
#ff-overlay{
  --ok:#4ade80;
  --err:#f87171;
  --gold:#C9A84C;
  position:fixed;inset:0;z-index:9000;
  /* fond avec halo purple — style Blast Club */
  background:
    radial-gradient(ellipse at 30% 40%, rgba(107,33,168,.12), transparent 60%),
    var(--bg);
  font-family:var(--font);
  color:var(--text);
  display:flex;flex-direction:column;
  overflow-y:auto;
}
#ff-overlay[hidden]{display:none}

/* ─── Header ──────────────────────────────────────────────── */
.ff-header{
  position:fixed;top:0;left:0;right:0;z-index:9001;
}
/* 3px progress bar gradient purple → pink */
.ff-progress-track{
  height:3px;
  background:rgba(255,255,255,.05);
  position:relative;
}
.ff-bar{
  height:100%;
  background:linear-gradient(90deg,var(--purple-light),var(--pink));
  width:0%;
  transition:width .3s ease-out;
}
.ff-header-meta{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:16px 32px;
}
.ff-brand{
  font-size:.65rem;
  font-weight:700;
  letter-spacing:.2em;
  text-transform:uppercase;
  color:var(--text-dim);
}
/* label affiché en or (#C9A84C) à droite de la barre */
.ff-step-label{
  font-size:.68rem;
  font-weight:600;
  color:var(--gold);
  letter-spacing:.08em;
  font-variant-numeric:tabular-nums;
}

/* ─── Main / Steps ────────────────────────────────────────── */
.ff-main{
  flex:1;
  display:flex;
  align-items:center;
  justify-content:center;
  /* espace généreux autour du contenu */
  padding:110px 48px 100px;
  width:100%;
}

/* conteneur relatif pour le grand numéro en arrière-plan */
.ff-step{
  position:relative;
  width:100%;
  max-width:560px;
  margin:0 auto;
  animation:ff-in .3s ease-out both;
}
.ff-step[hidden]{display:none!important}
.ff-step.leaving{
  animation:ff-out .22s ease-in both;
  pointer-events:none;
}

/* Grand numéro d'étape en arrière-plan — style Typeform */
.ff-step::before{
  content:attr(data-step);
  position:absolute;
  top:-60px;
  left:-16px;
  font-size:10rem;
  font-weight:700;
  line-height:1;
  color:var(--purple-light);
  opacity:.13;
  pointer-events:none;
  user-select:none;
  z-index:0;
  font-variant-numeric:tabular-nums;
  letter-spacing:-.05em;
}

/* le contenu passe au-dessus du numéro */
.ff-step-inner{
  position:relative;
  z-index:1;
}

/* transitions : translateY(40px) → 0 + fade, 300ms ease-out */
@keyframes ff-in{
  from{opacity:0;transform:translateY(40px)}
  to{opacity:1;transform:translateY(0)}
}
@keyframes ff-out{
  from{opacity:1;transform:translateY(0)}
  to{opacity:0;transform:translateY(-24px)}
}

/* ─── Typographie ─────────────────────────────────────────── */
/* Eyebrow : 0.7rem, letter-spacing 0.2em, uppercase */
.ff-eyebrow{
  font-size:.7rem;
  font-weight:600;
  letter-spacing:.2em;
  text-transform:uppercase;
  color:var(--purple-light);
  margin-bottom:24px;
}
/* Question dominante : Typeform-level */
.ff-q{
  display:block;
  font-size:clamp(2.8rem,5vw,4.2rem);
  font-weight:700;
  line-height:1.1;
  letter-spacing:-.04em;
  color:var(--text);
  margin-bottom:52px;
}
.ff-hint{
  font-size:.85rem;
  color:var(--text-muted);
  line-height:1.7;
  font-weight:300;
  margin-top:-38px;
  margin-bottom:32px;
}
.ff-opt{
  font-size:.45em;
  font-weight:400;
  color:var(--text-dim);
  letter-spacing:.04em;
  text-transform:uppercase;
}
.ff-req{color:var(--orange);font-size:.8em}

/* ─── Inputs — underline only, style Typeform ─────────────── */
.ff-input,.ff-textarea{
  display:block;
  width:100%;
  /* ZÉRO border-box */
  background:transparent;
  border:none;
  border-bottom:2px solid rgba(255,255,255,.15);
  border-radius:0;
  /* typographie large */
  color:var(--text);
  font-family:var(--font);
  font-size:1.4rem;
  font-weight:400;
  /* padding uniquement vertical */
  padding:16px 0;
  outline:none;
  transition:border-bottom-color .2s;
  resize:none;
  -webkit-appearance:none;
  width:100%;
}
/* focus : uniquement changer la couleur du border-bottom */
.ff-input:focus,.ff-textarea:focus{
  border-bottom-color:var(--purple-light);
  background:transparent;
  box-shadow:none;
}
.ff-input::placeholder,.ff-textarea::placeholder{
  color:var(--text-dim);
  font-weight:300;
}
/* Supprimer les spinners number */
.ff-input[type=number]::-webkit-inner-spin-button,
.ff-input[type=number]::-webkit-outer-spin-button{-webkit-appearance:none}
.ff-input[type=number]{-moz-appearance:textfield}

/* wrapper relatif pour le compteur de caractères */
.ff-field-wrap{position:relative;padding-bottom:28px}
.ff-counter{
  position:absolute;
  bottom:4px;right:0;
  font-size:.68rem;
  color:var(--text-dim);
  pointer-events:none;
  font-variant-numeric:tabular-nums;
  letter-spacing:.04em;
}

/* ─── Choices — cards sobres, style Blast Club ────────────── */
.ff-choices{
  display:flex;
  flex-wrap:wrap;
  gap:12px;
  margin-bottom:8px;
}
.ff-choices--duo{gap:16px}
.ff-choice{
  background:transparent;
  border:1px solid rgba(255,255,255,.1);
  border-radius:12px;
  color:var(--text-muted);
  font-family:var(--font);
  font-size:1rem;
  font-weight:500;
  /* padding 18px 28px spec exacte */
  padding:18px 28px;
  cursor:pointer;
  transition:border-color .2s,background .2s,color .2s;
  white-space:nowrap;
  -webkit-tap-highlight-color:transparent;
  letter-spacing:.01em;
}
.ff-choice:hover{
  border-color:rgba(147,51,234,.4);
  color:var(--text);
}
/* selected : border purple-light + bg rgba(147,51,234,0.15) */
.ff-choice.selected{
  border-color:var(--purple-light);
  background:rgba(147,51,234,.15);
  color:var(--text);
  font-weight:600;
}
.ff-choices--duo .ff-choice{
  flex:1;
  text-align:center;
  font-size:1.05rem;
  padding:20px 28px;
}

/* ─── Dropzone ────────────────────────────────────────────── */
.ff-dropzone{
  position:relative;
  border:1px dashed rgba(255,255,255,.12);
  border-radius:16px;
  background:transparent;
  transition:border-color .2s,background .2s;
  margin-bottom:4px;
  overflow:hidden;
}
.ff-dropzone:hover,.ff-dropzone.drag-over{
  border-color:rgba(147,51,234,.4);
  background:rgba(147,51,234,.05);
}
.ff-file-input{
  position:absolute;inset:0;
  opacity:0;cursor:pointer;
  width:100%;height:100%;
}
.ff-drop-idle{
  display:flex;flex-direction:column;
  align-items:center;gap:10px;
  padding:44px 24px;
  pointer-events:none;
}
.ff-drop-icon{
  width:52px;height:52px;
  background:rgba(255,255,255,.04);
  border:1px solid rgba(255,255,255,.08);
  border-radius:14px;
  display:flex;align-items:center;justify-content:center;
  color:var(--text-muted);
  margin-bottom:6px;
}
.ff-drop-label{font-size:.95rem;color:var(--text-muted);font-weight:500}
.ff-drop-sub{font-size:.8rem;color:var(--text-dim)}
.ff-drop-link{color:var(--purple-light);text-decoration:underline;text-underline-offset:3px}
.ff-drop-active{padding:28px 24px}
.ff-file-row{
  display:flex;align-items:center;gap:10px;
  color:var(--text-muted);
  margin-bottom:16px;
}
.ff-file-name{font-size:.88rem;color:var(--text);font-weight:500;word-break:break-all}
.ff-upload-track{
  height:3px;
  background:rgba(255,255,255,.06);
  border-radius:100px;
  overflow:hidden;
  margin-bottom:12px;
}
.ff-upload-fill{
  height:100%;
  background:linear-gradient(90deg,var(--purple-light),var(--pink));
  width:0%;
  border-radius:100px;
  transition:width .2s;
}
.ff-file-status{font-size:.78rem;font-weight:500;color:var(--text-dim)}
.ff-file-status.ok{color:var(--ok)}
.ff-file-status.err{color:var(--err)}

/* ─── Erreur inline ───────────────────────────────────────── */
.ff-error{
  min-height:22px;
  margin-top:14px;
  font-size:.78rem;
  color:var(--err);
  display:flex;align-items:center;gap:8px;
  letter-spacing:.02em;
}
.ff-error:not(:empty)::before{
  content:'';
  display:inline-block;
  width:4px;height:4px;
  background:var(--err);
  border-radius:50%;
  flex-shrink:0;
}

/* ─── Navigation ──────────────────────────────────────────── */
.ff-nav{
  display:flex;
  align-items:center;
  justify-content:space-between;
  margin-top:48px;
}
/* bouton Continuer : pill gradient purple → pink, positionné à droite */
.ff-btn-primary{
  display:inline-flex;align-items:center;gap:10px;
  background:linear-gradient(135deg,var(--purple),var(--pink));
  color:#fff;
  font-family:var(--font);
  font-size:.88rem;
  font-weight:600;
  /* padding 14px 36px spec exacte */
  padding:14px 36px;
  border:none;
  border-radius:100px;
  cursor:pointer;
  letter-spacing:.03em;
  text-transform:uppercase;
  transition:opacity .2s,transform .2s,box-shadow .2s;
  white-space:nowrap;
  margin-left:auto;
}
.ff-btn-primary:hover{
  opacity:.88;
  transform:translateY(-1px);
  box-shadow:0 12px 36px rgba(107,33,168,.4);
}
.ff-btn-primary:active{transform:translateY(0);box-shadow:none;opacity:1}
/* bouton Retour : texte pur, minimaliste */
.ff-btn-back{
  background:none;
  border:none;
  color:var(--text-dim);
  font-family:var(--font);
  font-size:.82rem;
  font-weight:400;
  padding:8px 0;
  cursor:pointer;
  transition:color .2s;
  letter-spacing:.06em;
  text-transform:uppercase;
}
.ff-btn-back:hover{color:var(--text-muted)}

/* ─── Keyboard hint ───────────────────────────────────────── */
.ff-kbd-bar{
  position:fixed;
  bottom:32px;left:50%;
  transform:translateX(-50%);
  font-size:.65rem;
  color:var(--text-dim);
  display:flex;align-items:center;gap:8px;
  pointer-events:none;
  z-index:9002;
  white-space:nowrap;
  opacity:0;
  transition:opacity .3s;
  letter-spacing:.08em;
  text-transform:uppercase;
}
.ff-kbd-bar.visible{opacity:1}
kbd{
  display:inline-flex;align-items:center;
  background:rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.1);
  border-radius:5px;
  padding:3px 8px;
  font-family:var(--font);
  font-size:.68rem;
  color:var(--text-muted);
  letter-spacing:0;
  text-transform:none;
}

/* ─── Bouton fermer ───────────────────────────────────────── */
.ff-close{
  position:fixed;top:20px;right:24px;
  z-index:9010;
  width:32px;height:32px;
  background:transparent;
  border:1px solid rgba(255,255,255,.1);
  border-radius:50%;
  color:var(--text-dim);
  display:flex;align-items:center;justify-content:center;
  cursor:pointer;
  transition:border-color .2s,color .2s;
}
.ff-close:hover{
  border-color:rgba(255,255,255,.25);
  color:var(--text);
}

/* ─── Écran final ─────────────────────────────────────────── */
.ff-step--final .ff-step-inner{text-align:center}
/* supprime le grand numéro "30" en fond sur le dernier écran */
.ff-step--final::before{display:none}
.ff-final-state{display:flex;flex-direction:column;align-items:center;gap:20px}
.ff-spinner{
  width:36px;height:36px;
  border:1.5px solid rgba(255,255,255,.08);
  border-top-color:var(--purple-light);
  border-radius:50%;
  animation:spin .8s linear infinite;
}
@keyframes spin{to{transform:rotate(360deg)}}
.ff-final-label{
  font-size:.75rem;
  color:var(--text-dim);
  font-weight:400;
  letter-spacing:.12em;
  text-transform:uppercase;
}
/* ✦ icône succès */
.ff-success-glyph{
  font-size:2.8rem;
  background:linear-gradient(135deg,var(--purple-light),var(--pink));
  -webkit-background-clip:text;
  background-clip:text;
  -webkit-text-fill-color:transparent;
  line-height:1;
  margin-bottom:4px;
}
.ff-success-title{
  font-size:clamp(2.8rem,5vw,4rem);
  font-weight:700;
  letter-spacing:-.04em;
  color:var(--text);
  line-height:1.1;
}
.ff-success-body{
  font-size:.95rem;
  color:var(--text-muted);
  line-height:1.8;
  font-weight:300;
  max-width:420px;
}
.ff-success-body strong{color:var(--text);font-weight:500}
.ff-err-body{
  font-size:.9rem;
  color:var(--text-muted);
  line-height:1.8;
  text-align:center;
}
.ff-err-body a{color:var(--purple-light);text-decoration:none}
.ff-err-body a:hover{text-decoration:underline}
.ff-btn-retry{
  background:transparent;
  border:1px solid rgba(255,255,255,.1);
  color:var(--text-muted);
  font-family:var(--font);
  font-weight:500;
  font-size:.78rem;
  letter-spacing:.1em;
  text-transform:uppercase;
  padding:12px 28px;
  border-radius:100px;
  cursor:pointer;
  transition:border-color .2s,color .2s;
}
.ff-btn-retry:hover{border-color:rgba(255,255,255,.25);color:var(--text)}

/* ─── Responsive ──────────────────────────────────────────── */
@media(max-width:640px){
  .ff-main{padding:96px 28px 96px}
  .ff-q{font-size:2.2rem;margin-bottom:36px;letter-spacing:-.03em}
  .ff-input,.ff-textarea{font-size:1.15rem}
  .ff-choice{padding:14px 20px;font-size:.92rem}
  .ff-btn-primary{padding:14px 28px}
  .ff-kbd-bar{display:none}
  .ff-header-meta{padding:14px 20px}
  .ff-step::before{font-size:7rem;top:-48px;left:-8px}
}
</style>

<script define:vars={{ BETA_CODE, WEBHOOK_URL, UPLOAD_URL }}>
(function(){
  const TOTAL = 29;
  let current = 0;
  let startTime = null;
  const csrf = crypto.randomUUID();
  const uploads = { 27:{url:'',filename:''}, 29:{url:'',filename:''} };
  const SK = 'ff_data';

  const overlay  = document.getElementById('ff-overlay');
  const bar      = document.getElementById('ff-bar');
  const lbl      = document.getElementById('ff-step-label');
  const kbdBar   = document.getElementById('ff-kbd-bar');
  const closeBtn = document.getElementById('ff-close');

  // ── Helpers ──────────────────────────────────────────────
  const step  = n => document.querySelector(`.ff-step[data-step="${n}"]`);
  const inp   = n => document.getElementById(`ff-${n}`);
  const err   = n => document.getElementById(`ff-err-${n}`);
  const setErr= (n,m) => { const e=err(n); if(e) e.textContent=m; };
  const clrErr= n => { const e=err(n); if(e) e.textContent=''; };
  const choice= n => { const g=document.getElementById(`ff-${n}`); if(!g) return ''; const s=g.querySelector('.ff-choice.selected'); return s?s.dataset.value:''; };

  function progress(n){
    const pct = n===0 ? 3 : Math.round((n/TOTAL)*100);
    bar.style.width = pct+'%';
    lbl.textContent = n===0 ? 'Accès' : `${n} / ${TOTAL}`;
  }

  function reqLabels(){
    const rev = choice(17) === 'Oui';
    [18,19].forEach(n => {
      const el = document.getElementById(`ff-${n}-req-label`);
      if(el) el.innerHTML = rev
        ? '<span class="ff-req" style="font-size:.55em;color:var(--orange)">*</span>'
        : '<span class="ff-opt">— optionnel</span>';
    });
  }

  function saveSession(){
    const d={};
    for(let i=1;i<=29;i++){
      const el=inp(i);
      if(!el||el.type==='file') continue;
      d[`f${i}`]=el.value;
    }
    document.querySelectorAll('.ff-choice.selected').forEach(b=>{
      const g=b.closest('.ff-choices');
      if(g) d[`c${g.id}`]=b.dataset.value;
    });
    sessionStorage.setItem(SK,JSON.stringify(d));
  }

  function loadSession(){
    try{
      const raw=sessionStorage.getItem(SK);
      if(!raw) return;
      const d=JSON.parse(raw);
      for(const [k,v] of Object.entries(d)){
        if(k.startsWith('f')){
          const el=document.getElementById(`ff-${k.slice(1)}`);
          if(el&&el.type!=='file') el.value=v;
        } else if(k.startsWith('c')){
          const g=document.getElementById(k.slice(1));
          if(g) g.querySelectorAll('.ff-choice').forEach(b=>{ if(b.dataset.value===v) b.classList.add('selected'); });
        }
      }
      syncCounters();
    }catch(e){}
  }

  function syncCounters(){
    [[7,140],[8,400],[9,400],[20,500]].forEach(([n,max])=>{
      const el=inp(n); const c=document.getElementById(`ff-count-${n}`);
      if(el&&c) c.textContent=`${el.value.length} / ${max}`;
    });
  }

  // ── Keyboard hint visibility ─────────────────────────────
  function updateKbd(n){
    const s=step(n);
    if(!s){kbdBar.classList.remove('visible');return;}
    const hasText=s.querySelector('.ff-input,.ff-textarea');
    if(hasText) kbdBar.classList.add('visible');
    else kbdBar.classList.remove('visible');
  }

  // ── Validation ───────────────────────────────────────────
  const EMAIL_RE=/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
  const URL_RE=/^https?:\/\/.+\..+/;

  function validate(n){
    clrErr(n);
    const el=inp(n);
    const required=()=>{ if(!el||!el.value.trim()){setErr(n,'Ce champ est requis.');return false;} return true; };
    switch(n){
      case 0: if(!el||el.value.trim()!==BETA_CODE){setErr(0,'Code invalide. Contactez contact@flaynn.fr.');return false;} return true;
      case 1:case 3:case 4:case 6:case 10:case 11:case 13:case 22:case 24:case 26: return required();
      case 2: if(!required()) return false; if(!EMAIL_RE.test(el.value.trim())){setErr(2,'Email invalide.');return false;} return true;
      case 7:case 8:case 9:case 20: return required();
      case 12: if(!el||el.value===''){setErr(12,'Ce champ est requis.');return false;} return true;
      case 16:case 17:case 21:case 23: if(!choice(n)){setErr(n,'Sélectionnez une option.');return false;} return true;
      case 18:case 19: if(choice(17)==='Oui'&&(!el||el.value==='')){setErr(n,'Ce champ est requis.');return false;} return true;
      case 27: if(!uploads[27].url){setErr(27,'Uploadez votre pitch deck.');return false;} return true;
      case 28: { const v=el?el.value.trim():''; if(v&&!URL_RE.test(v)){setErr(28,'URL invalide (commencer par https://).');return false;} return true; }
      default: return true;
    }
  }

  // ── Navigation ───────────────────────────────────────────
  function showStep(n, dir=1){
    const prev=step(current);
    if(prev&&!prev.hidden){
      prev.classList.add('leaving');
      setTimeout(()=>{ prev.hidden=true; prev.classList.remove('leaving'); }, 220);
    }
    setTimeout(()=>{
      const next=step(n);
      if(!next) return;
      next.hidden=false;
      current=n;
      progress(n);
      reqLabels();
      updateKbd(n);
      overlay.scrollTo(0,0);
      const f=next.querySelector('.ff-input:not([type=file]),.ff-textarea');
      if(f) setTimeout(()=>f.focus(),50);
      saveSession();
    }, dir===-1?0:60);
  }

  function goNext(n){
    if(!validate(n)) return;
    if(n===29){submit();return;}
    showStep(n+1,1);
  }
  function goBack(n){
    showStep(n<=1?0:n-1,-1);
  }

  // ── Open / Close ─────────────────────────────────────────
  function openForm(){
    overlay.hidden=false;
    document.body.style.overflow='hidden';
    startTime=Date.now();
    current=0;
    document.querySelectorAll('.ff-step').forEach((s,i)=>{ s.hidden=i!==0; });
    progress(0);
    updateKbd(0);
    const f=inp(0); if(f) setTimeout(()=>f.focus(),80);
    loadSession();
  }
  function closeForm(){
    overlay.hidden=true;
    document.body.style.overflow='';
    kbdBar.classList.remove('visible');
  }

  window.openFounderForm=openForm;
  closeBtn.addEventListener('click',closeForm);
  overlay.addEventListener('click',e=>{ if(e.target===overlay) closeForm(); });
  document.addEventListener('keydown',e=>{ if(e.key==='Escape'&&!overlay.hidden) closeForm(); });

  // ── Button delegation ─────────────────────────────────────
  overlay.addEventListener('click',e=>{
    const nb=e.target.closest('.ff-btn-next');
    const bb=e.target.closest('.ff-btn-back');
    if(nb){ goNext(parseInt(nb.dataset.step)); return; }
    if(bb){ goBack(parseInt(bb.dataset.step)); return; }
  });

  overlay.addEventListener('keydown',e=>{
    if(e.key!=='Enter') return;
    if(e.target.tagName==='TEXTAREA') return;
    if(e.target.closest('.ff-choices')) return;
    const s=step(current);
    if(!s) return;
    const btn=s.querySelector('.ff-btn-next');
    if(btn) btn.click();
  });

  // ── Choice selection ──────────────────────────────────────
  overlay.addEventListener('click',e=>{
    const c=e.target.closest('.ff-choice');
    if(!c) return;
    const g=c.closest('.ff-choices');
    if(!g) return;
    g.querySelectorAll('.ff-choice').forEach(b=>b.classList.remove('selected'));
    c.classList.add('selected');
    clrErr(current);
    saveSession();
  });

  // ── Counters ──────────────────────────────────────────────
  [[7,140],[8,400],[9,400],[20,500]].forEach(([n,max])=>{
    const el=inp(n);
    const c=document.getElementById(`ff-count-${n}`);
    if(el&&c) el.addEventListener('input',()=>{ c.textContent=`${el.value.length} / ${max}`; saveSession(); });
  });
  for(let i=1;i<=29;i++){
    const el=inp(i);
    if(el&&el.type!=='file') el.addEventListener('input',()=>saveSession());
  }

  // ── File upload ───────────────────────────────────────────
  function setupUpload(sn){
    const fileInput=inp(sn);
    const zone=document.getElementById(`ff-file-zone-${sn}`);
    const idle=document.getElementById(`ff-drop-idle-${sn}`);
    const active=document.getElementById(`ff-file-info-${sn}`);
    const nameEl=document.getElementById(`ff-file-name-${sn}`);
    const fillEl=document.getElementById(`ff-upload-bar-${sn}`);
    const statEl=document.getElementById(`ff-file-status-${sn}`);
    const MAX=15*1024*1024;
    const TYPES27=['.pdf'];
    const TYPES29=['.pdf','.doc','.docx','.xls','.xlsx','.ppt','.pptx','.jpg','.png'];

    if(!fileInput||!zone) return;

    zone.addEventListener('dragover',e=>{ e.preventDefault(); zone.classList.add('drag-over'); });
    zone.addEventListener('dragleave',()=>zone.classList.remove('drag-over'));
    zone.addEventListener('drop',e=>{ e.preventDefault(); zone.classList.remove('drag-over'); if(e.dataTransfer.files.length) upload(e.dataTransfer.files[0]); });
    fileInput.addEventListener('change',()=>{ if(fileInput.files.length) upload(fileInput.files[0]); });

    function upload(file){
      clrErr(sn);
      const allowed=sn===27?TYPES27:TYPES29;
      const ext='.'+file.name.split('.').pop().toLowerCase();
      if(!allowed.includes(ext)){ setErr(sn,`Format non accepté. Formats : ${allowed.join(', ')}`); return; }
      if(file.size>MAX){ setErr(sn,'Fichier trop volumineux (max 15 Mo).'); return; }

      idle.hidden=true; active.hidden=false;
      nameEl.textContent=`${file.name}  ·  ${(file.size/1024/1024).toFixed(1)} Mo`;
      fillEl.style.width='0%';
      statEl.textContent='Upload en cours…'; statEl.className='ff-file-status';

      const fd=new FormData(); fd.append('file',file);
      const xhr=new XMLHttpRequest();
      xhr.open('POST',UPLOAD_URL);
      xhr.upload.addEventListener('progress',e=>{ if(e.lengthComputable) fillEl.style.width=Math.round(e.loaded/e.total*100)+'%'; });
      xhr.addEventListener('load',()=>{
        if(xhr.status>=200&&xhr.status<300){
          try{
            const res=JSON.parse(xhr.responseText);
            uploads[sn]={url:res.url,filename:file.name};
            fillEl.style.width='100%';
            statEl.textContent='✓ Fichier prêt'; statEl.className='ff-file-status ok';
          } catch(e2){ statEl.textContent='Erreur de réponse.'; statEl.className='ff-file-status err'; setErr(sn,'Réessayez.'); }
        } else { statEl.textContent=`Erreur ${xhr.status}`; statEl.className='ff-file-status err'; setErr(sn,'Erreur upload.'); }
      });
      xhr.addEventListener('error',()=>{ statEl.textContent='Erreur réseau'; statEl.className='ff-file-status err'; setErr(sn,'Erreur réseau.'); });
      xhr.send(fd);
    }
  }
  setupUpload(27); setupUpload(29);

  // ── Submit ────────────────────────────────────────────────
  async function submit(){
    const hp=document.getElementById('ff-honeypot');
    if(hp&&hp.value) return;
    const dur=Math.round((Date.now()-startTime)/1000);
    if(dur<10) return;

    showStep(30,1);
    const spinner=document.getElementById('ff-final-spinner');
    const success=document.getElementById('ff-final-success');
    const errorEl=document.getElementById('ff-final-error');
    spinner.hidden=false; success.hidden=true; errorEl.hidden=true;

    const v=n=>{ const el=inp(n); return el?el.value.trim():''; };
    const payload={
      code_acces:v(0), nom_fondateur:v(1), email:v(2), pays:v(3), ville:v(4), telephone:v(5),
      nom_startup:v(6), pitch_une_phrase:v(7), probleme:v(8), solution:v(9),
      secteur:v(10), type_client:v(11), tam_usd:parseFloat(v(12))||0, estimation_tam:v(13),
      acquisition_clients:v(14), concurrents:v(15), stade:choice(16), revenus:choice(17),
      mrr:parseFloat(v(18))||0, clients_payants:parseInt(v(19))||0,
      pourquoi_vous:v(20), equipe_temps_plein:choice(21), priorite_6_mois:v(22),
      montant_leve:choice(23), jalons_18_mois:v(24), utilisation_fonds:v(25), vision_5_ans:v(26),
      pitch_deck_url:uploads[27].url, pitch_deck_filename:uploads[27].filename,
      lien_demo:v(28),
      doc_supplementaire_url:uploads[29].url, doc_supplementaire_filename:uploads[29].filename,
      submitted_at:new Date().toISOString(), csrf_token:csrf,
      fill_duration_seconds:dur, honeypot:hp?hp.value:''
    };

    try{
      const res=await fetch(WEBHOOK_URL,{ method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload) });
      spinner.hidden=true;
      if(res.ok){
        document.getElementById('ff-success-email').textContent=payload.email;
        success.hidden=false;
        sessionStorage.removeItem(SK);
      } else { errorEl.hidden=false; }
    } catch(e){ spinner.hidden=true; errorEl.hidden=false; }
  }

  document.getElementById('ff-retry').addEventListener('click',()=>showStep(29,-1));

  // ── Init ─────────────────────────────────────────────────
  progress(0); reqLabels(); updateKbd(0);
})();
</script>
